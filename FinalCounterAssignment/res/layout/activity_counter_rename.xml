<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:paddingBottom="@dimen/activity_vertical_margin"
    android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    tools:context=".CounterRename" >

    <EditText
        android:id="@+id/newName"
        android:layout_width="fill_parent"
        android:layout_height="wrap_content"
        android:scrollbars="horizontal"
        android:hint="Enter the new counter name"
        android:singleLine="true" />

    <Button
        android:id="@+id/Bconfirm"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignLeft="@+id/newName"
        android:layout_alignRight="@+id/newName"
        android:layout_below="@+id/newName"
        android:layout_marginTop="25dp"
        android:text="Confirm" />

    <Button
        android:id="@+id/BtoEdit"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignLeft="@+id/Bconfirm"
        android:layout_alignRight="@+id/Bconfirm"
        android:layout_below="@+id/Bconfirm"
        android:layout_marginTop="58dp"
        android:text="Cancel" />

</RelativeLayout>
